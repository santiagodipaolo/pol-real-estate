# Handoff — Data Moat + Model Improvements (2026-03-13)

## Qué se hizo esta sesión

### Detail Scraping — deployed (PRs #8, #9)
- Pipeline de enrich completo: `scrape_detail()` en Zonaprop y Argenprop
- Fix async (`asyncio.run()` → `await` directo)
- ~186 listings enriquecidos en prod, 1016 pendientes
- Features: 13→21 (floor, amenities, condition, orientation, etc.)

### Model Improvements — deployed (PRs #10, #11)
- Log-transform del target (reduce skew en distribución de precio/m²)
- IQR → percentile-based outlier filter (P2-P98)
- Lat/lng como features directas + surface_per_room + log_barrio_median
- XGBoost tuning: depth 4, lr 0.05, 300 estimators, L1/L2 regularization
- **Resultado**: Sale 28.8% → 23.0% MAE, Rent 30.0% → 24.8% MAE

### Data Moat — deployed (PR #12)
- **Price history**: tabla `listing_price_history` registra cada cambio de precio
- **De-listing detection**: `deactivate_stale_listings(7 days)` corre en pipeline
- **Scale scraping**: max_pages 10→50 (GitHub Actions, admin, Celery)
- Migration 004: listing_price_history + deactivated_at column
- Pipeline completo probado: scrape + deactivate + retrain

## Estado actual prod
- Backend: https://backend-production-f7a52.up.railway.app
- Frontend: https://pol-real-estate.vercel.app
- ~1400 listings activos (377 sale, ~1000 rent)
- Sale MAE: 23.0% (377 samples, 25 features)
- Rent MAE: 24.8% (982 samples, 25 features)
- Enrich: ~186 done, ~1016 pending
- Pipeline cada 6h via GitHub Actions (50 pages)
- Price history vacío (se llena en próximos ciclos cuando precios cambien)
- Deactivation: 0 aún (7 días threshold, va a empezar a detectar ~2026-03-19)

## Branch
`feat/detail-scraping` — todos los PRs mergeados a main (#8-#12)

## Qué falta hacer

### P0 — Inmediato
1. **Anti-403 Zonaprop**: solo scrapeó 25 listings de sale. Necesita mejor stealth (rotate user agents, delays más largos, o proxy). Argenprop funciona bien.
2. **Enrich masivo**: quedan 1016 listings sin enriquecer. Correr batches de 100-200 periódicamente o configurar enrich en el pipeline cron (`enrich=true`).
3. **Verificar pipeline cron**: confirmar que GitHub Actions tiene los secrets (`BACKEND_URL`, `ADMIN_API_KEY`) configurados y está corriendo cada 6h.

### P1 — Data Moat (continuar Fase 1)
4. **Más fuentes**: agregar Properati y/o MercadoLibre Inmuebles como scrapers. Más fuentes = más listings + mejor cross-source dedup.
5. **Barrio snapshots**: verificar que el cron de snapshots diarios está corriendo (03:00 UTC). Si no, activarlo. Es clave para trend analysis.
6. **Seed initial price history**: para listings existentes, insertar un registro inicial con el precio actual como baseline (sino el historial empieza vacío hasta el primer cambio).

### P2 — Market Intelligence (Fase 2)
7. **Rental yield por barrio**: cruzar sale vs rent por barrio para calcular yield estimado. Query simple pero muy valioso.
8. **Trend detection**: con snapshots diarios, calcular % cambio mensual de precio/m² por barrio. Detectar barrios subiendo/bajando.
9. **Comparable engine**: dado un listing, encontrar los 5 más similares (distancia euclidiana en feature space o simplemente mismo barrio + tipo + superficie similar).
10. **Price history API**: endpoint GET /listings/{id}/price-history para mostrar evolución de precio en el frontend.

### P3 — Dashboard de Experto (Fase 3)
11. **Vista de mercado**: dashboard con trends por barrio, yields, alertas
12. **Watchlists**: usuario puede seguir barrios o listings específicos
13. **Alertas**: "Palermo bajó 5% este mes", "nueva oportunidad en tu zona"
14. **Reportes PDF**: reporte de valuación exportable

### P4 — Cobertura
15. **GBA**: expandir scraping a Gran Buenos Aires (Vicente López, San Isidro, Tigre)
16. **Córdoba / Rosario**: mercados grandes con poca transparencia de datos

## Notas técnicas
- Zonaprop bloquea agresivamente (403) con más de ~10 páginas. Necesita proxies o mejor stealth.
- `_run_enrich_sync` no existe en admin.py (bug del pipeline con enrich=true). Funciona via endpoint `/admin/enrich` directo.
- Celery no está activo en Railway (no hay Redis). Pipeline corre via GitHub Actions + admin API.
- El modelo mejora ~1pp por cada ~100 listings enriquecidos con detail data.
